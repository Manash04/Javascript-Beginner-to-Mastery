// Changing text content
titleElement.textContent = "New Title";

// Changing attribute value
firstParagraph.setAttribute("class", "new-paragraph");

// Changing styles
firstParagraph.style.color = "blue";
